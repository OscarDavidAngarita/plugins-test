name: shared_preferences_platform_interface
description: Personalized common platform interface for the shared_preferences plugin.
repository: https://github.com/OscarDavidAngarita/plugins-test/tree/main/packages/shared_preferences/shared_preferences_platform_interface

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.10.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
